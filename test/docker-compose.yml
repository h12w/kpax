zookeeper:
  image: h12w/zookeeper:latest
  container_name: ccfea3b2a73aee522ffbf41723f38cddecc6a404
  ports:
    - "2181"
kafka:
  image: h12w/kafka:latest
  ports:
    - "9092"
  links:
    - zookeeper:zk
  environment:
    #KAFKA_ADVERTISED_HOST_NAME: 192.168.59.103
    KAFKA_ADVERTISED_HOST_NAME: 192.168.1.120
    KAFKA_DELETE_TOPIC_ENABLE: "true"
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
